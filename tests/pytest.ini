[pytest]
addopts = --maxfail=1 --tb=short
markers =
    api: tests liés à l'API Wigor
    gui: tests liés à l'interface Tkinter
    models: tests des objets ScheduleItem
    index: tests du parsing HTML
